import { render, _render, renderComponent } from './render'
import { setAttribute } from './dom.js';
import createElement, { VNode } from './create-element'

export {
  setAttribute,
  _render,
  createElement,
  VNode,
  renderComponent,
}

export default {
  render
}